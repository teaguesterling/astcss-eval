{
  "_comment": "Surface-form ablation task set. One fixture, 20 mutation tasks. The SELECTOR is identical across all five surfaces -- only the wrapper syntax changes -- so the arm difference isolates the surface. Per-task metadata: arity (0/1/2) and arg_verbatim (does the argument appear literally in the request) exist so selector/op/arg can be scored separately and so the arity-0 subset can be reported alone, which is the only subset with zero transcription credit.",
  "fixture": "fixtures/py-variety/sample_app.py",
  "language": "python",
  "tasks": [
    {"id": "T01", "tier": "M1", "arity": 0, "arg_verbatim": null,
     "request": "delete the search_users method",
     "selector": ".fn#search_users", "op": "remove", "args": []},
    {"id": "T02", "tier": "M1", "arity": 0, "arg_verbatim": null,
     "request": "remove the delete_user method entirely",
     "selector": ".fn#delete_user", "op": "remove", "args": []},
    {"id": "T03", "tier": "M1", "arity": 0, "arg_verbatim": null,
     "request": "drop the print call inside bulk_import",
     "selector": ".fn#bulk_import .call#print", "op": "remove", "args": []},
    {"id": "T04", "tier": "M1", "arity": 0, "arg_verbatim": null,
     "request": "delete the Config class",
     "selector": ".class#Config", "op": "remove", "args": []},
    {"id": "T05", "tier": "M1", "arity": 0, "arg_verbatim": null,
     "request": "unwrap the try block inside the retry function",
     "selector": ".fn#retry .try", "op": "unwrap", "args": []},
    {"id": "T06", "tier": "M1", "arity": 0, "arg_verbatim": null,
     "request": "unwrap the try block in main",
     "selector": ".fn#main .try", "op": "unwrap", "args": []},

    {"id": "T07", "tier": "M2", "arity": 1, "arg_verbatim": true,
     "request": "rename the search_users method to safe_search",
     "selector": ".fn#search_users", "op": "rename", "args": ["safe_search"]},
    {"id": "T08", "tier": "M2", "arity": 1, "arg_verbatim": true,
     "request": "rename the UserService class to AccountService",
     "selector": ".class#UserService", "op": "rename", "args": ["AccountService"]},
    {"id": "T09", "tier": "M2", "arity": 1, "arg_verbatim": true,
     "request": "rename the transform function to normalize",
     "selector": ".fn#transform", "op": "rename", "args": ["normalize"]},
    {"id": "T10", "tier": "M2", "arity": 1, "arg_verbatim": true,
     "request": "add a verbose=False parameter to the get_user method",
     "selector": ".fn#get_user", "op": "addParam", "args": ["verbose=False"]},
    {"id": "T11", "tier": "M2", "arity": 1, "arg_verbatim": true,
     "request": "add a timeout parameter to validate_email",
     "selector": ".fn#validate_email", "op": "addParam", "args": ["timeout"]},
    {"id": "T12", "tier": "M2", "arity": 1, "arg_verbatim": true,
     "request": "remove the params parameter from the execute method",
     "selector": ".fn#execute", "op": "removeParam", "args": ["params"]},
    {"id": "T13", "tier": "M2", "arity": 1, "arg_verbatim": true,
     "request": "add retries=3 as an argument to the create_connection call",
     "selector": ".call#create_connection", "op": "addArg", "args": ["retries=3"]},
    {"id": "T14", "tier": "M2", "arity": 1, "arg_verbatim": true,
     "request": "remove the timeout keyword argument from the create_connection call",
     "selector": ".call#create_connection", "op": "removeArg", "args": ["timeout"]},

    {"id": "T15", "tier": "M3", "arity": 1, "arg_verbatim": true,
     "request": "insert setup_logging() at the top of the main function",
     "selector": ".fn#main", "op": "prepend", "args": ["setup_logging()"]},
    {"id": "T16", "tier": "M3", "arity": 1, "arg_verbatim": true,
     "request": "add self.url = None at the end of the close method",
     "selector": ".fn#close", "op": "append", "args": ["self.url = None"]},

    {"id": "T17", "tier": "M4", "arity": 2, "arg_verbatim": true,
     "request": "wrap the export_users method, putting \"try:\" before it and \"except Exception: pass\" after it",
     "selector": ".fn#export_users", "op": "wrap", "args": ["try:", "except Exception: pass"]},
    {"id": "T18", "tier": "M4", "arity": 2, "arg_verbatim": true,
     "request": "inside the search_users method, replace the text \"SELECT *\" with \"SELECT id, name\"",
     "selector": ".fn#search_users", "op": "replaceWith", "args": ["SELECT *", "SELECT id, name"]},

    {"id": "T19", "tier": "M5", "arity": 2, "arg_verbatim": true, "anchor_is_selector": true,
     "request": "in the load method, insert validate(self.data) just before the return statement",
     "selector": ".fn#load", "op": "insertBefore", "args": [".jump", "validate(self.data)"]},
    {"id": "T20", "tier": "M5", "arity": 2, "arg_verbatim": true, "anchor_is_selector": true,
     "request": "in the connect method, insert self.ready = True just after the create_connection call",
     "selector": ".fn#connect", "op": "insertAfter", "args": [".call#create_connection", "self.ready = True"]}
  ],
  "applicability_checks": [
    ".fn#load .jump",
    ".fn#connect .call#create_connection"
  ]
}
